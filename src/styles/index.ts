import {StyleSheet} from 'react-native';

const darkColor = '#176B87';
const lightColor = '#DAFFFB';

const styles = StyleSheet.create({
  statusBar: {
    backgroundColor: darkColor,
  },
});

export default styles;
export {darkColor, lightColor};
